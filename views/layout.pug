html(lang="en", data-theme="light")
    head
        title
            | #{permission_requested} login page
        <meta name="viewport" content="width=device-width, initial-scale=1">
        // css: https://thecascade.dev/article/least-amount-of-css/
        style.
            html { color-scheme: light dark; }
            body {
                font-family: system-ui;
                font-size: 1.25rem;
                line-height: 1.5;
            }
            img,svg,video {
                max-width: 100%;
                display: block;
            }
            main {
                max-width: min(70ch, 100% - 4rem);
                margin-inline: auto;
            }
            .panel {
                position: absolute;
                margin: auto;
                top: -50px;
                right: 0;
                bottom: 0;
                left: 0;
                width: 300px;
                height: 300px;
                border-radius: 5px;
            }
            input {
                width: 100%;
                font-size: 1.25rem;
            }
            .error {
                color: red;
                border-radius: 5px;
                background: light-dark(#eee, #444);
                padding: 1em;
                text-align: center;
            }
    body
        main.container
            block content